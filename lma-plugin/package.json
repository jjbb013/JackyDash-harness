{
  "name": "@lma/standalone",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "LMA 物流推广智能体系统 —— 独立版（Node + SQLite + 内置 AI 助手，无需 DeepSeek Harness）",
  "exports": {
    "./package.json": "./package.json"
  },
  "scripts": {
    "start": "node --experimental-strip-types --no-warnings src/main.ts",
    "build:client": "tsc -p tsconfig.client.json && DSH_BUILD_FACE=client tsdown --config tsdown.config.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/react": "~18.3.1",
    "react": "^18.2.0"
  },
  "peerDependencies": {}
}
